[project]
name = "scrap"
version = "0.1.0"
description = "Web scraping, cleaning, rewriting and export pipeline"
readme = "README.md"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"
