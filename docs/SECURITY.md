# Безопасность

- Все секреты только в `/opt/scrap/.env` (chmod 600)
- `.env` не коммитить; шаблон — `.env.example`
- Не логировать токены и ключи
- Не хранить секреты в markdown
- Ключи из чата Cursor — осознанная передача; записывать только в `.env`
- Перед правками nginx/systemd — backup конфигов
- Не трогать `/hermes`, `/opt/cliproxyapi` без явной команды
